CREATE TABLE Store(
 ORDER_NO INT PRIMARY KEY,
  CODE VARCHAR(20),
  ITEM CHAR(20),
  QUANTITY  INT ,
  PRICE INT,
  DISCOUNT VARCHAR(20),
  MRP INT
  );
  INSERT INTO STORE VALUES
  (001,'JWCCPN','PEN',20,104,'5%',1000),
  (002,'BOOKNNN','BOOK',15,578,'5%',1000),
  (003,'PENCILQQ','PENCIL',100,333,'20%',100000);
  SELECT * FROM STORE;
  SELECT MOD(PRICE,9) AS REM FROM STORE ;
  SELECT POWER(PRICE,2) AS POW FROM STORE;
  SELECT ROUND(PRICE DIV 7) AS ROUND FROM STORE;
  
  DROP TABLE STORE